import styled from 'styled-components';

const Container = styled.div`
  width: 1000px;
  max: 100%;
  padding: 0 20px;
  margin: 0 auto;
  &.card: {
    color: red;
  }
`;

export default Container;
